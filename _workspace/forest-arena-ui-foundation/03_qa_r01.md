# QA r01

- 상태: pass
- 검토 대상: 브랜드·Android 계약, 24개 화면, 61개 에셋, Penpot 하네스, Phase 0 회귀.
- 필수 검사: `sh -n scripts/start-penpot-mcp.sh`, `git diff --check`, `./scripts/verify-harness.sh`, `./scripts/verify.sh`, Android export·APK badging.
- 조건부 검사: 연결된 에뮬레이터가 있을 때 설치·가로 실행·중단/복귀.
- 명시적 미실행: Penpot 서버/외부 파일, 이미지 생성, Godot 비전투 화면, 실제 물리 기기.

## 결과

- `sh -n scripts/start-penpot-mcp.sh scripts/export-debug-android.sh scripts/verify-android-package.sh scripts/verify-android-emulator.sh scripts/verify-harness.sh scripts/verify.sh`: pass.
- JSON parse와 CSV 행 수 61: pass.
- `tests/ui_design_contract.gd`: pass. 최초 실행에서 JSON 숫자 배열 타입 비교가 엄격해 해상도 검사만 실패했고 각 값을 정수 변환하는 계약 동등 비교로 수정한 뒤 통과했다.
- `./scripts/verify-harness.sh`: pass. 15개 스킬, 새 경로·ID, Penpot route/template, 활성 이전 이름과 legacy 렌더링 용어 부재를 확인했다.
- `./scripts/verify.sh`: pass. Phase 0 smoke, CharacterData, 승인 모션, 공격 콘셉트, UI 설계와 하네스 계약이 모두 통과했다.
- `git diff --check`: pass.
- `./scripts/export-debug-android.sh`: pass. APK 내부 package/label/API/ABI/가로 화면과 개발 전용 경로 부재를 확인했다.
- `./scripts/verify-android-emulator.sh build/android/ForestArena-debug.apk`: pass. `emulator-5554`에서 설치, cold start, background, hot resume와 활성 2400×1080 landscape viewport를 확인했다.
- APK: `build/android/ForestArena-debug.apk`, 171806014 bytes, SHA-256 `f01d71f3b66a43ad0857591fbb238bec27168b9d39d7608532729cf7efdb6b5f`.

## 미실행·이연

- 실제 물리 Android 기기: 미검증이며 pass로 간주하지 않는다.
- Penpot MCP 서버와 외부 `Forest_Arena_UI` 파일: 계획에 따라 미실행.
- 이미지 생성과 24개 Godot 비전투 화면: 후속 작업.

알려진 차단 결함은 없다.
