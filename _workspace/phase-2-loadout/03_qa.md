# QA

상태: pass (로컬 자동 검증).

- `tests/phase2_loadout_contract.gd`: catalog 고유성, schema, Character-only, fixture, 부모→자식, conflict, 누락 ID, source immutability.
- `tests/phase2_runtime_wiring.gd`: main scene selection→profile→fighter 주입.
- Phase 1 결정론 SHA: `bbf88e54628a7ef086b3021a43352089e2f36916365475cc8cfbbd762ffcc7a2` 유지.
- APK export는 `build/android/ForestArena-debug.apk`를 생성했고 ZIP 무결성, AndroidManifest 존재, `arm64-v8a`/`x86_64` native library를 확인했다.
- SDK `aapt`는 생성 APK를 `Invalid file`로 오인했고 `apkanalyzer`도 build-tools 탐색 오류로 실패해 자동 package contract는 환경 도구 문제로 미확정이다.
- 연결된 Android emulator가 없어 install·landscape·touch·pause/resume은 미검증이다. 실제 Android 기기도 미검증이다.
