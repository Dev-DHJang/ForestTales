# QA r01

## 결과

- `./scripts/verify-harness.sh`: 통과. 팀 명세에서 읽은 14개 역할과 실제 스킬 목록, 한국어 구조, 통합 책임, 리소스·외형·온라인 경계를 확인했다.
- `./scripts/verify.sh`: 통과. Godot headless 로드, Phase 0 기반 smoke, Phase 1 전투·입력·결정론 SHA, Phase 2 로드아웃 계약·런타임 주입, 캐릭터·모션·UI·리소스 검증을 확인했다.
- `git diff --check`: 통과.
- `./scripts/export-debug-android.sh`, `./scripts/verify-android-package.sh`: 통과. `build/android/ForestArena-debug.apk`와 Android 식별자를 확인했다.
- 활성 문서에서는 제거된 세 역할, 고정 역할 수, `현재 Phase 0` 표현을 검사했다. 기존 ADR과 `_workspace` 감사 기록은 의도적으로 검사 대상에서 제외했다.

## 미검증

ADB daemon이 실행 중이지 않아 연결 에뮬레이터 생명주기 검증은 실행하지 못했다. 실제 Galaxy S23 Ultra 등 물리 Android 기기 검증도 미검증이다.
