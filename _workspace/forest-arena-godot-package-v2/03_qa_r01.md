# QA r01

- 상태: pass
- 설치 보고서: `forest_arena_install_report.json`의 `verify_return_code`는 0이며 337개 logical asset과 22개 품질 의존 asset을 확인했다.
- `python tools/forest_arena/verify_godot_resources.py --project-root .`: pass.
- `godot --headless --path . --editor --quit`: pass; 신규 리소스를 import하고 `ForestArenaResources` Autoload가 있는 프로젝트를 로드했다.
- `sh -n scripts/verify-harness.sh scripts/verify.sh`, `git diff --check`: pass.
- `./scripts/verify-harness.sh`: pass; 17개 역할, Godot Autoload·registry·profile·package harness contract를 확인했다.
- `./scripts/verify.sh`: pass; Phase 0 smoke, Phase 1 전투·결정론·입력, 캐릭터·모션·공격·UI 계약 및 확장 하네스를 모두 통과했다.

## 미실행

- 실제 Android 기기·에뮬레이터 설치 및 생명주기 검증은 실행하지 않았다. 전체 회귀 중 ADB daemon은 연결되지 않았지만 Godot 자동 검증에는 영향을 주지 않았다.
