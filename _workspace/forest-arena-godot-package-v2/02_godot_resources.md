# Godot 리소스 산출물

- `res://forest_arena/`에 337개 logical asset과 22개 품질 의존 asset의 3단계 변형이 설치됐다.
- `ForestArenaResources`는 registry/profile을 읽고 설정을 `user://forest_arena_graphics.cfg`에 저장한다.
- 패키지 규칙대로 버튼 문구는 Godot native text로 유지하고, 기존 UI·캐릭터 산출물에는 경로 변경이나 복사를 하지 않는다.
