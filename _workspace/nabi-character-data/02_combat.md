# 전투 경계 검토

- 나비의 역할은 `기동형 근접 압박가`이며, 능력치는 향후 플레이테스트용 기본 프로필이다.
- `nabi-claw-jab`, `nabi-pounce-slash`, `nabi-guard-rake`는 의미 슬롯 ID만 가진다.
- 이 Resource는 AttackData, startup/active/recovery, 피해, 넉백, hitbox, 상태 전이, 승패를 소유하지 않는다.
- `concept_image`는 표현 전용이며 파일의 배경 특성은 전투 결과에 영향을 주지 않는다.
