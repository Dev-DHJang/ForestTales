# Phase 0 QA r02

## 판정

로컬·Android 에뮬레이터 범위 pass. 실제 물리 기기와 원격 develop 통합은 미확인/blocked로 분리한다.

## r01 수정 검증

| r01 결함 | 결과 | 증거 |
| --- | --- | --- |
| 실제 최소 SDK 24 | pass | Gradle custom build와 `gradle_build/min_sdk=29`; `aapt`에서 `sdkVersion:'29'` 확인 |
| 개발 증적 APK 포함 | pass | `_workspace`, `docs`, `tests`, `.agents` export 제외 및 APK asset 부재 검사 |
| 수동 에뮬레이터 절차 | pass | `scripts/verify-android-emulator.sh` 한 명령으로 설치·COLD 시작·Home·HOT 재개·top resumed 확인 |

## 최종 자동 검증

- `./scripts/verify.sh` — pass.
- `./scripts/export-debug-android.sh` — pass: export, 서명, API 29/36, package ID, 앱 이름, 가로 feature와 ABI 검사.
- `./scripts/verify-android-emulator.sh` — pass: `emulator-5554` 설치, COLD 시작, 백그라운드와 재개.
- `tests/phase0_smoke.gd` — pass: 프로젝트/장면/입력/safe edge/멀티포인터 해제.
- `./scripts/verify-harness.sh` — pass.

## 미확인과 차단

- 실제 물리 Android 기기의 설치·멀티터치·노치·중단/복귀·10분 성능은 수행하지 않았으며 pass가 아니다.
- Git 저장소와 origin/develop이 없어 feature branch, commit, PR/병합과 원격 롤백 증적은 blocked다.
- 후속 사용자 응답으로 ADR-001/002/003/011은 accepted가 됐다. ADR-006/007은 실제 기기 측정 전 proposed 상태다.
