# Phase 0 UI 기록

- `TouchCommandSource`가 화면 하단 구역을 일곱 의미 입력으로 변환한다.
- 포인터별 입력을 추적해 멀티터치를 지원하고 경계 이탈 시 이전 입력을 해제한다.
- 포커스 손실과 앱 중단 때 합성 입력을 해제한다.
- `expand` 화면 비율로 20:9에서도 레터박스 없이 표현하며 논리 좌표에 맞춰 입력 영역을 다시 계산한다.
- 현재 비율은 기기 측정 전 임시값이며 safe area 보정과 사용자 배치는 다음 검증에서 다룬다.
