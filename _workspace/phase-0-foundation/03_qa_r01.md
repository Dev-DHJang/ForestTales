# Phase 0 QA r01

## 판정

fix — 최초 구현의 로컬·에뮬레이터 동작은 통과했지만 APK 계약과 패키징 경계 수정이 필요하다.

## 자동 검증

- `./scripts/verify.sh` — pass.
- Godot 4.7.1 headless editor import/boot — pass.
- `tests/phase0_smoke.gd` — pass: 의미 입력 7개, Android 가로·expand·ETC2/ASTC, 필수 장면 노드, safe edge, 동일 행동 멀티포인터 유지와 전체 입력 해제.
- `./scripts/verify-harness.sh` — pass.
- Android debug export — APK 생성 자체는 pass.

## 에뮬레이터 수동 회귀

- `AndroidEmulatorColdStart`: `com.foresttales.game/com.godot.game.GodotAppLauncher` COLD 시작 — pass.
- `AndroidLandscapeWideScreen`: 2400×1080, arm64 에뮬레이터에서 가로·edge-to-edge·20:9 배경과 터치 안전 여백 — pass([화면 캡처](evidence/android-emulator-2400x1080.png)).
- `AndroidBackgroundHotResume`: Home 전환 뒤 같은 task HOT 재개 — pass.
- 위 설치·COLD 시작·Home·HOT 재개·top resumed 검사는 `scripts/verify-android-emulator.sh`로 재현 가능하다.
- 프로세스 로그: Godot main loop 시작 및 OnPause→OnStart→OnResume 확인. 앱 크래시나 GDScript 오류 없음.

## 경계 검토

- TouchCommandSource는 의미 InputMap만 생산하며 전투 결과를 계산하지 않는다 — pass.
- 도형 실루엣·배경과 CollisionShape2D가 분리됐다 — pass.
- Phase 1 공격·판정·피해·승패를 구현하지 않았다 — pass.
- 임시 package ID, 터치와 성능값은 proposed ADR이다 — pass.

## 미확인과 차단

- 실제 물리 Android 기기 설치·터치·노치·중단/복귀·10분 성능은 수행하지 않았으며 pass가 아니다.
- 폴더가 Git 저장소가 아니어서 요구되는 feature branch, commit, PR와 origin/develop 병합 증적을 만들 수 없다.
- 에뮬레이터에서 화면 중단 시 EGL surface 경고가 있었으나 HOT 재개와 렌더링은 정상 복구됐다. 물리 기기에서 재확인한다.

## 수정 요구

1. 생성 APK의 실제 `minSdkVersion`이 24여서 제품 기준 Android 10(API 29)과 불일치한다.
2. `export_filter=all_resources`가 `_workspace`의 QA 화면 증적을 APK asset으로 포함한다.
3. 설치·COLD 시작·중단·HOT 재개 절차가 수동 명령 기록에만 있고 단일 재현 스크립트가 없다.

재검증은 `03_qa_r02.md`에서 수행한다.
