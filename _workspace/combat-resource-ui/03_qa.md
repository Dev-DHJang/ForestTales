# QA 증적

## 자동 검증

- `python3 tools/forest_arena/verify_godot_resources.py --project-root .`: PASS, 345 logical assets, 23 quality-dependent assets.
- `godot --headless --path . --script res://tests/phase1_input_ui.gd`: PASS.
  - 배경·HUD 패널·재시작·D-pad·행동 버튼 텍스처 로드.
  - D-pad 방향과 행동 버튼 press/release 상태 전환.
  - high·medium·low 배경 전환과 공통 버튼 리소스 유지.
  - 네이티브 Godot 행동 라벨과 누락 경고 부재.
- `./scripts/verify.sh`: PASS, Phase 0–2·캐릭터·전투·UI 계약·하네스 전체 회귀 통과.

## 이름 있는 수동 회귀

- `COMBAT_RESOURCE_UI_1280X720_RENDER`: PASS.
  - `godot --path . --write-movie /tmp/forest-arena-resource-ui-final.png --fixed-fps 1 --quit-after 1`로 1280×720 프레임을 렌더링했다.
  - 배경, HUD 패널, 재시작, D-pad, 다섯 행동 버튼과 링아웃 경계의 표시 및 겹침을 확인했다.

## 미검증

- `adb devices`에 연결된 에뮬레이터나 물리 기기가 없어 Android 설치·가로 실행·생명주기 검증은 수행하지 않았다.
- 실제 Android 기기 통과로 기록하지 않는다.
