# QA r01

상태: pass (코드·로컬·Android 에뮬레이터). 원격 통합은 별도 version-control 단계에서 확인한다.

- `git diff --check`: pass.
- 모든 shell script `sh -n`: pass.
- `./scripts/verify.sh`: pass.
- 계약: 세 CharacterData 연결, 고유 공격 ID, 전체 기술군, 정확한 기본 수치, 묘령 3-hit를 통과했다.
- 전투: 이동·대시 고정, attack tick, 단일 버퍼, whiff 연계, hit 전용 분기, 추격 점프, 5방향 공중 입력, 위 특수/no-op을 통과했다.
- 결정론: 같은 snapshot과 intent stream의 최종 SHA-256 일치.
- 판정/매치: post-hit 피해 넉백, DI 10°, 단발 중복·자가 타격 방지, 45/60 tick 복귀, 반복 서든데스와 단독 패배를 통과했다.
- 입력/UI: 네 방향·dead zone·드래그·멀티터치·포인터 해제·pause tick 정지·HUD 비권위·카메라 제한을 통과했다.
- Android export/package/emulator lifecycle 및 터치 진단: pass.
- 승인 PNG, SpriteFrames와 manifest는 변경하지 않았다.
- Galaxy S23 Ultra: 물리 기기 미검증(accepted ADR에 따른 비필수 항목).
