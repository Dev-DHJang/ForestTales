# QA 기록

## 자동

- `godot --headless --path . --editor --quit`: 통과.
- `tests/phase0_smoke.gd`: 통과.
- `tests/phase1_combat_contract.gd`: 통과.
- `tests/phase1_match_rules.gd`: 통과. 동시 최종 링아웃의 반복 서든데스와 비동시 결과, 피해·경직 기본식을 검사한다.
- `git diff --check`: 실행 예정.

## 미완료

- 전체 `./scripts/verify.sh`, Android debug export와 에뮬레이터 lifecycle 검증은 아직 종료 증적을 남기지 않았다.
- Galaxy S23 Ultra 물리 기기 검증은 ADR 기준에 따라 미검증이다.
