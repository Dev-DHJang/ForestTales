# 전투 UI

- 왼쪽 하단은 dead zone과 우세 축을 가진 단일 4방향 패드이며 드래그 전환·포인터별 해제·행동 멀티터치를 지원한다.
- HUD는 fighter/match snapshot만 표시하고 재시작은 `reset_match`만 호출한다.
- 도형 파이터는 외곽선·facing 선·상태 문자·무적 깜박임과 디버그 hitbox로 색 이외의 상태 단서를 제공한다.
- Camera2D는 두 파이터 중점과 간격을 따라가며 zoom을 0.78–1.0으로 제한한다.
