# Harness Bootstrap QA r01

## 판정

pass

## 수용 기준 검증

| 기준 | 결과 | 증거 |
| --- | --- | --- |
| 필수 문서·팀 명세·13개 스킬 | pass | ./scripts/verify-harness.sh가 경로와 역할 수를 검사 |
| 스킬 frontmatter·이름·필수 섹션 | pass | 모든 SKILL.md의 name과 디렉터리명 및 네 필수 섹션 일치 |
| 2D 전환과 구형 렌더링 용어 제거 | pass | 생성 문서·스킬 대상 grep 검사 결과 없음 |
| 자리표시자 치환 | pass | ForestTales와 forest-tales를 사용하며 미치환 패턴 없음 |
| 실제 기기·게임 실행 | 명시적 이연 | Godot 프로젝트가 없는 Phase 0 이전 상태이며 다음 Phase에서 재개 |

## 라우팅 시나리오

1. 정상 전투 변경 — pass
   - team-spec의 이동·공격·판정 행은 combat와 qa를 필수로 지정한다.
   - combat는 AttackData·상태·판정을, 애니메이션과 UI는 표현만 소유한다.
2. 공용 계약 변경 — pass
   - contracts는 Resource·공용 ID 변경 시 생산 전에 01_contract.md를 요구한다.
   - team-spec의 Resource·ID·저장 행도 같은 선행 관계를 사용한다.
3. QA 수정 회차 — pass
   - fix는 생산자, redo는 product 또는 contracts로 반환한다.
   - 동일 원인의 수정은 최대 두 회차 뒤 접근과 역할을 재분류한다.
4. Phase 7 이전 온라인 요청 — pass
   - team-spec, contracts, multiplayer와 network가 accepted ADR과 versioned 계약 전 구현을 금지한다.
   - 조건 미충족 시 blocked와 재개 조건을 인수인계한다.

## 경계 일치 검사

- README 링크 대상과 실제 번호 문서·팀 명세 경로가 일치한다.
- 팀 명세의 13개 스킬명이 실제 frontmatter와 일치한다.
- 작업 증적 파일명과 pass/fix/redo/blocked 상태 어휘가 일치한다.
- 제품 문서의 현재 상태는 Phase 0 게임 구현 전이며 구현되지 않은 기능을 완료로 표현하지 않는다.

## 실행 기록

- ./scripts/verify-harness.sh — pass.
- /bin/sh -n scripts/verify-harness.sh — pass.
- grep 기반 자리표시자, 링크 대상, 네 시나리오와 금지어 대조 — pass.
- 첫 보조 검사 래퍼는 zsh의 특수 path 변수를 루프 변수로 사용해 PATH가 바뀌면서 중단되었다. 파일 결함이 아니며 변수명을 target_file로 바꾼 동일 검사에서 pass했다.

## 미확인

- Godot 부트, 게임 테스트, Android export·설치·터치·성능과 실제 기기 검증은 이번 범위가 아니며 pass로 간주하지 않는다.
